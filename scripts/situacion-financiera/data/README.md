# Catálogo local SF (T03/T04/T05)

Generado por `pipeline/seed_huellas.py` y `run_corte.py --persist-local`.

- `catalogo_local/tipos.json` — tipos conocidos  
- `catalogo_local/huellas.json` — huellas aprobadas  
- `catalogo_local/cortes/` · `staging/` — lotes (pueden ser grandes; regenerables)

No es Supabase prod. MIG-203 = schema LAB.
